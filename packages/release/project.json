{
  "name": "release",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/release/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "test": {
      // The test here is an integration test, which uses the built CLI
      "dependsOn": ["build"]
    }
  }
}
